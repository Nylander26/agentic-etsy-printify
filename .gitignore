node_modules/
dist/
.env
*.tsbuildinfo

# Pipeline output
output/
approved/
rejected/
pending-review/
research-results/
data/etsy-packs/

# DB (incl. WAL sidecar files)
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db

# Apify scrape cache (24h TTL, regenerated on demand)
cache/

# Auth tokens (never commit)
.etsy-tokens.json
test-image.png

# Claude
.claude/settings.*
.claude/settings.local.json
# npx-skills creates absolute-path symlinks here — machine-specific, never commit
.claude/skills/